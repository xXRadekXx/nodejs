<!DOCTYPE html>
<html lang="en">
<head>
  <h1>Rawr's Database</h1>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rawr's database</title> 
    <link rel="stylesheet" href="styles.css">
    <a href="/NEWuser2" target="_blank" rel="nofollow noopener" class="btn btn-primary">Vytvoření nového uživatele</a>
    <br>
    <a href="/deleteuser" target="_blank" rel="nofollow noopener" class="btn btn-primary">Smazání uživatele</a>
</head>
<body>
    <table>
      <% var cssLink = '/styles.css'; %>
      
        <% for(var i=0; i < results.length; i++) { %>
          <tr>
            <td>
              <%= results[i].ID_Student %>
            </td>
            <td>
              <%= results[i].First_Name %>
            </td>
            <td>
              <%= results[i].Last_Name %>
            </td>
            <td>
              <%= results[i].Age %>
            </td>
          </tr>
          <% } %>
      </table>
      <style> 
        table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      border: 2px solid #dddddd;
      text-align: left;
      padding: 5px;
    }

    th {
      background: linear-gradient(to right, #0f3f99, #00c48c);
      color: rgb(0, 0, 0);
    }

    tr:nth-child(even) {
      background: linear-gradient(to right, #4e8cff, #00c48c);
      color: white;
    }

    tr:nth-child(odd) {
      background: linear-gradient(to right, #4e8cff, #00c48c);
    }

    
    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 20px;
      overflow: hidden;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }

    th, td {
      padding: 15px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    th {
      background-color: #3498db;
      color: white;
      border-top-left-radius: 10px;
      border-top-right-radius: 10px;
    }

    tr:hover {
      background-color: #f5f5f5;
    }

    tr:nth-child(even) {
      background-color: #ecf0f1;
    }

    tr:last-child td:first-child {
      border-bottom-left-radius: 10px;
    }

    tr:last-child td:last-child {
      border-bottom-right-radius: 10px;
    }

  </style>
</body>
</html>